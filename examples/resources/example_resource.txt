*** Settings ***
Library           SSHLibrary

*** Variables ***
${HOST}           localhost
${USERNAME}       test
${PASSWORD}       test
${PROMPT}         $
${LS}             ls -a

*** Keywords ***
Open connection and log in
    Open Connection    ${HOST}    prompt=${PROMPT}
    Login    ${USERNAME}    ${PASSWORD}
